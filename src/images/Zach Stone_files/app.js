// typed script for name 

var nameText = {
    strings: ['Zach Stone ^800', 'ザック・ストーン ^800'],
    typeSpeed: 100,
    startDelay: 10,
    backSpeed: 70,
    loop: true,
    loopCount: Infinity,
};

var typed = new Typed('.type', nameText);

// event listner to make sure the button doesnt move when the h1 is empty